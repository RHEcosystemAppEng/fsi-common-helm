{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Schema pusher configuration values schema",
  "type":"object",
  "required": [
    "kafka",
    "service",
    "naming",
    "topics",
    "schemas"
  ],
  "properties": {
    "kafka": {
      "type": "object",
      "description": "Kafka info",
      "required": [
        "bootstrap"
      ],
      "properties": {
        "bootstrap": {
          "type": "string",
          "description": "Bootstrap URL",
          "$ref": "#/$defs/url"
        },
        "certificates": {
          "type": "object",
          "description": "Certificates information",
          "oneOff": [
            {
              "required": [
                "server"
              ]
            },
            {
              "required": [
                "user"
              ]
            }
          ],
          "properties": {
            "server": {
              "type": "object",
              "description": "Server certificate information",
              "required": [
                "secret"
              ],
              "properties": {
                "secret": {
                  "type": "string",
                  "description": "The name of the secret containing the server p12 store"
                }
              }
            },
            "user": {
              "type": "object",
              "description": "User certificate information",
              "required": [
                "secret"
              ],
              "properties": {
                "secret": {
                  "type": "string",
                  "description": "The name of the secret containing the user p12 store"
                }
              }
            }
          }
        }
      }
    },
    "service": {
      "type": "object",
      "description": "Service registry info",
      "required": [
        "registry"
      ],
      "properties": {
        "registry": {
          "type": "string",
          "description": "Registry URL",
          "$ref": "#/$defs/url"
        }
      }
    },
    "naming": {
      "type": "object",
      "description": "Subject naming configuration",
      "required": [
        "strategy"
      ],
      "properties": {
        "strategy": {
          "type": "string",
          "description": "The selected naming strategy",
          "enum": ["topic", "record", "topic_record"]
        }
      }
    },
    "topics": {
      "type": "array",
      "description": "A list of topics for pushing the messages with",
      "items": {
        "type": "string"
      }
    },
    "schemas": {
      "type": "array",
      "description": "A list of base64 representations of schemas",
      "items": {
        "type": "string",
        "contentEncoding": "base64",
        "contentMediaType": "application/gzip"
      }
    },
    "labels": {
      "type": "object",
      "description": "Labels for implementing with the created resources."
    }
  },
  "$defs": {
    "url": {
      "format": "uri",
      "pattern": "^https?://"
    }
  }
}
